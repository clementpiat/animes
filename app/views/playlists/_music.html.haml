-# music

- id = "music-playlist-#{music.id}"
%div{id: id}
    - if music.type == :custom
        = link_to music.youtube_video_url, "https://youtube.com/watch?v=#{music.youtube_video_id}", 
            target: '_blank', class: 'youtube_url'
    - else
        = link_to image_tag("musics/#{music.youtube_video_id}.jpg", size: "160x100"), "https://youtube.com/watch?v=#{music.youtube_video_id}", 
            target: '_blank',  style: 'float: left; margin-right: 5vw; margin-bottom: 1vw;'
        %h6= music.name

    = link_to remove_music_playlist_path(music_id: music.id, id: @playlist.id), method: :post, remote: true,
                                class: 'btn btn-danger btn-sm', style: 'float: right;', data: {confirm: 'Are you sure?'} do
        %i.fa.fa-times